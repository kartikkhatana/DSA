#include <iostream>
#include <map>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int regions, categories;
        cin >> regions >> categories;

        int participants;
        cin >> participants;
        int array[n];
        for (int i = 0; i < n; i++)
        {
            cin >> array[i];
        }
        cout << array[2];
    }

    return 0;
}
